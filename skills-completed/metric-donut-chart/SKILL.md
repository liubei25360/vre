---
name: metric-donut-chart
description: Animated SVG donut progress ring with neon glow and ease-out counter. The ring fills from 0 to target with a drop-shadow glow, while the center number counts up visually. Perfect for showing completion %, scores, or goal progress.
category: svg-data
tags: [donut, chart, ring, progress, glow, svg, circular, counter]
dependencies: [remotion]
version: 1.0.0
---

# MetricDonutChart

SVG donut progress ring that fills with neon glow and counts up. Props: `value`, `max`, `label`, `primaryColor`, `strokeWidth`, `size`.

Built for `vibe-coding-video`.
