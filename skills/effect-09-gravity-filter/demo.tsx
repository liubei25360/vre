import React from "react";
import { Effect09GravityFilter, EFFECT_09_DURATION_FRAMES } from "./Effect09GravityFilter";

export const Effect09GravityFilterDemo: React.FC = () => {
  return <Effect09GravityFilter />;
};

export { EFFECT_09_DURATION_FRAMES as DURATION };
