import React from "react";
import { Effect07NoiseSilence, EFFECT_07_DURATION_FRAMES } from "./Effect07NoiseSilence";

export const Effect07NoiseSilenceDemo: React.FC = () => {
  return <Effect07NoiseSilence />;
};

export { EFFECT_07_DURATION_FRAMES as DURATION };
