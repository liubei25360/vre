# 🚀 Next-Gen Vertical Video Agentic Skill Library 
**次世代竖屏短视频 Agent Skill 资产库 (Remotion / R3F / CSS3)**

> 本文档定义了用于自动化生成降维打击级别技术流短视频的 React 组件库。核心原则：**极度注重空间感 (Z-Axis)、光影交互、数学缓动曲线 (Spring Physics)，且在消费级 GPU 上保证极速渲染。**

---

## 📁 目录 (Table of Contents)

1. [四大核心基建 (The Core 4)](#1-四大核心基建-the-core-4)
2. [架构与导航 (Structure & HUD)](#2-架构与导航-structure--hud)
3. [理论与概念 (Theory & Concept)](#3-理论与概念-theory--concept)
4. [实操与界面 (Execution & UI)](#4-实操与界面-execution--ui)
5. [高阶矢量数据可视化 (SVG & Data Art)](#5-高阶矢量数据可视化-svg--data-art)
6. [高阶 HTML 交互 (HTML/CSS Interactive)](#6-高阶-html-交互-htmlcss-interactive)

---

## 1. 四大核心基建 (The Core 4)

最基础且最高频使用的组件，支撑起 80% 的视频高级感。

### `<SpatialGlassContainer />` (2.5D 空间玻璃容器)
*   **功能描述：** 替代生硬矩形截图的终极载体，带有物理折射、发光边框和多层环境阴影。
*   **适用场景：** 装载网页截图、代码片段等所有需要展示的 2D 素材。
*   **死磕细节：** `backdrop-filter: blur(20px)`，配合微小偏转 `rotateX(5deg) rotateY(-8deg)`，Z 轴缓慢呼吸浮动。阴影需带有环境色扩散。

### `<SmartCameraRig />` (智能运镜控制器)
*   **功能描述：** 模拟 Screen Studio 的丝滑局部放大平移。
*   **适用场景：** 引导观众视线，在竖屏中看清横向代码或 UI 按钮。
*   **死磕细节：** 绝对弃用 Linear 匀速动画。完全通过 Remotion `spring()` 函数计算 `transform: scale() translate()`，保证苹果级的起停缓冲质感。

### `<NeonTypewriter />` (极客光晕打字机)
*   **功能描述：** 高级代码/Prompt 逐字打印机，带有赛博朋克光效。
*   **适用场景：** 屏幕留白处强调核心指令或金句。
*   **死磕细节：** 新字符出现瞬间必须带有高亮爆闪（Glow flare）。结尾必须跟随带有 `drop-shadow` 辉光的矩形闪烁光标。

### `<ZAxisTransition />` (Z轴深潜转场)
*   **功能描述：** 榨干 9:16 纵深感，利用 Z 轴进行画面替换的转场引擎。
*   **适用场景：** 章节切换、理论到实操的过渡。
*   **死磕细节：** 旧画面向 Z 轴深处（Scale < 1）坠落并伴随运动模糊（Motion Blur）；新画面从摄像机后方（Scale > 1）飞入。

---

## 2. 架构与导航 (Structure & HUD)

位于 Z 轴最前方（Layer 1），缓解竖屏信息焦虑，增强教程专业度。

### `<CyberProgressTracker />` (全息赛博进度条)
*   **功能描述：** 紧贴屏幕边缘的光带与节点网络。
*   **适用场景：** SOP 流程节点指示（Step 1 -> Step 2）。
*   **死磕细节：** 电流蔓延动画，到达当前节点时发生高亮爆闪并展开微小发光文本。

### `<KineticChapterTitle />` (巨物冲击标题)
*   **功能描述：** 纯文本、极具暴力美学的粗体大字报。
*   **适用场景：** 章节开启、核心震撼点抛出（如“0成本”）。
*   **死磕细节：** 短时间居中霸屏显示，带有强烈的缩放冲击力和镜头微震（Camera Shake）。

### `<DataMetricCounter />` (高频跳动数据盘)
*   **功能描述：** 高级数字滚动器。
*   **适用场景：** 强调播放量、代码行数、节约时间等数值指标。

---

## 3. 理论与概念 (Theory & Concept)

将白板图解转化为数字空间立体艺术。

### `<SpatialConceptCard />` (空间理论投影卡)
*   **功能描述：** 竖屏中错落漂浮的多张毛玻璃理论卡片。
*   **死磕细节：** 前后堆叠时，必须配合高斯模糊（`filter: blur()`）展现出逼真的镜头景深（Depth of Field）。

### `<InsightTextDrop />` (金句/洞察高亮坠落)
*   **功能描述：** 核心干货以优雅的排版从天而降。
*   **死磕细节：** 高级衬线体排版，辅以微弱的渐变流光扫过文字表面。

---

## 4. 实操与界面 (Execution & UI)

### `<MacOsGlassWindow />` (拟物化玻璃视窗)
*   **功能描述：** 自动为截图套上精致的 Mac 视窗外壳。
*   **死磕细节：** 红黄绿小圆点，暗色透明 Title Bar，底层继承 `<SpatialGlassContainer />` 的材质与阴影。

### `<NeonTerminal />` (极客代码终端机)
*   **功能描述：** 专为代码展示设计的暗黑终端。
*   **死磕细节：** 关键代码行高亮（其他代码自动变暗 50%）。内置 Prism.js 语法高亮映射引擎。

### `<3DFlipContainer />` (正反面空间翻转器)
*   **功能描述：** 极具视觉张力的对比容器。正面 Prompt，背面代码。
*   **死磕细节：** Y 轴 `rotateY` 180度翻转，背部元素隐藏（`backface-visibility: hidden`）。

### `<HeroDeviceMockup />` (等距 3D 设备蒙版)
*   **功能描述：** 成果展示载体。
*   **死磕细节：** 使用 Isometric 2.5D 视角的手机或屏幕 PNG 图片，通过 `clip-path` 将录屏视频完美嵌入设备屏幕中。

### `<ConfettiBurst />` (极简粒子庆祝)
*   **功能描述：** 代码跑通/成果展示时的情绪增强动效。
*   **死磕细节：** 极客风的简单线条与几何点，带金色发光，不使用花哨碎纸片。

---

## 5. 高阶矢量数据可视化 (SVG & Data Art)

利用纯数学与代码，极速渲染无损的复杂逻辑动效。

### `<GlobalDataRoute />` (全息流光节点图)
*   **功能描述：** 赛博网格上的数据穿梭动画。
*   **死磕细节：** 使用 SVG `stroke-dasharray` 配合渐变高光拖尾，途径节点触发呼吸灯光效。

### `<LogicPathTracer />` (逻辑树/架构图自发光绘制)
*   **功能描述：** 随讲解自动生长出的架构图连线。
*   **死磕细节：** `stroke-dashoffset` 随时间归零，配合颜色变量区分 If/Else 逻辑分支路线。

### `<SvgMorphingIcon />` (超级矢量形变引擎)
*   **功能描述：** 图标之间的无缝液体形变。
*   **死磕细节：** 操控 SVG `d` 路径数据，实现如“Bug -> 盾牌”的极简降维转场。

### `<CyberRadarScanner />` (大模型思考/搜索雷达蒙版)
*   **功能描述：** 填充无聊等待时间的镭射光特效。
*   **死磕细节：** 发光扫描线周期性扫过代码/界面区域，扫过之处底层内容出现极强荧光高亮。

### `<DynamicMapTracer />` (第一视角路径追踪仪)
*   **功能描述：** 镜头跟随发光点移动的动态路线图。
*   **死磕细节：** 利用代码实时计算光点坐标，并动态更新摄像机的 `scale` 与 `translate` 实现永远居中的追车视角。

### `<VectorSpacePlotter />` (多维坐标投影散点阵)
*   **功能描述：** 带 2.5D 透视系的散点图表与坐标系。
*   **死磕细节：** 节点不仅自身有霓虹光晕，悬停时会向坐标轴投射带有流光动画的虚线辅助线。

### `<NeuralProcessingBox />` (算法黑盒扫描机)
*   **功能描述：** 模拟千亿参数计算的黑盒动画。
*   **死磕细节：** 容器内绘制复杂的神经网络半透明线条，镭射扫描线高频上下循环扫视。

### `<MetricDonutChart />` & `<TelemetryDashboard />` (实时遥测全息仪表板)
*   **功能描述：** 数据跟随动画进度真实跳动更新的 HUD 仪表。
*   **死磕细节：** 非线性的数值跳动（`Ease-out`），配以极客发光环形进度条。

---

## 6. 高阶 HTML 交互 (HTML/CSS Interactive)

展现纯正 Web 前端美学，复刻顶级 SaaS 官网的丝滑互动感。

### `<BentoGridMorpher />` (流体便当盒网格编排器)
*   **功能描述：** 基于 CSS Grid 的多板块自适应展开动画。
*   **死磕细节：** 核心版块面积弹性膨胀，其他区块如流体般被挤压重排，内部元素采用级联延迟载入（Staggered fade-in）。

### `<SpotlightGlassCard />` (边缘巡航探照灯卡片)
*   **功能描述：** 发光边框自动沿物理边缘绕行的魔法卡片。
*   **死磕细节：** 利用 `conic-gradient` 与 CSS Mask 模拟虚拟高光游走，即使卡片 3D 偏转，巡航光依然完美贴合边缘。

### `<InlineCodeDiff />` (无缝代码重构引擎)
*   **功能描述：** 模拟原生 IDE 插入/修改代码的物理裂开动画。
*   **死磕细节：** 高度从 0 到 `auto` 的缓动，拒绝淡入淡出，新代码在上下推开的缝隙中高亮浮现。

### `<ParallaxTiltContainer />` (引力视差交互面板)
*   **功能描述：** 随虚拟引力发生 3D 偏转和玻璃高光移动的顶级画框。
*   **死磕细节：** 容器物理倾斜的同时，内部的蒙版（Glare Effect）反向移动，营造极限逼真的玻璃材质折射感。

### `<InfiniteDataMarquee />` (Z轴纵深数据滚轮)
*   **功能描述：** 具有强烈 3D 透视感的无限循环数据流瀑布。
*   **死磕细节：** 利用极强的 CSS Perspective，元素沿 Y 轴不同速度滚动，并在上下两端被 `linear-gradient` 黑色蒙版无缝羽化吞噬。

---
**[EOF] 结束符**